import React from 'react'
import { useParams } from 'react-router-dom'

function DonorContainer() {
//   const {donorID} = useParams();
//   const {body} = getBody(donorID);
  
  return (
    <div>{'Ameer is here'}</div>
  )
}
export default DonorContainer;